import { SettingsTablePageSkeleton } from "@/components/skeletons/page-skeletons";

/** Generic settings content fallback — subnav stays (layout); only pane skeletons. */
export default function Loading() {
  return <SettingsTablePageSkeleton />;
}
